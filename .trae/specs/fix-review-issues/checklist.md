- [x] 所有 `REVIEW.md` 中仍成立的问题都有对应修复、状态更新或 `// CONCERN` 说明。
- [x] 活体陷阱释放后不会永久失效，捕获状态不再只依赖静态集合。
- [x] 流星弩成功发射后耐久消耗正确，创造模式行为无异常。
- [x] 铜管网络跨区块重新加载后不会因旧 `networkId` 产生明显网络分裂或幽灵网络。
- [x] 铜管 `transfer()` 注释、调用方和实际行为一致，出口满时无意外丢失。
- [x] 属性纹饰效果和铜管/端点方块重复逻辑已在不改变行为的前提下收敛。
- [x] 剩余玩家可见硬编码文本已迁移到翻译键，并补充 `zh_cn.json`。
- [x] tick 热路径中审查报告点名的不必要分配或遍历已降低，未引入新的 Side 安全风险。
- [x] 新增或修改的类、方法、关键 NeoForge 逻辑包含精简中文注释。
- [x] `./gradlew.bat build --no-daemon` 通过。
- [x] 代码审查无 Critical/Important 阻塞项。
- [ ] 已按要求提交并推送到 `origin main`，且远程内容已验证与本地一致。
